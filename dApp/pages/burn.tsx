import { useRouter } from 'next/router';

export const getStaticProps = async () => {
  return {
    props: {},
  };
};

const Burn = () => {
  const router = useRouter();

  return (
    <></>
  );
};

export default Burn;
